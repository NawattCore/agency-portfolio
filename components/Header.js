// Since the navigation is integrated into the HeroSlider component,
// you can either keep this simple or remove it entirely
export default function Header() {
  return null; // Navigation is now handled by HeroSlider
}